# Failing (But somehow accepted by ´file´):
# ----------------------------------------------------------
# Read-length does not cover the matching part.
############################################################
0 regex>9<	[0-9]{4}@[a-d]{4} text/x-MimeUtil-RegExp
